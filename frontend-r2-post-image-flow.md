## 7. 投稿一覧で画像を表示する

投稿取得APIから `image_url` が返るようになったら、投稿一覧画面で表示します。

```tsx
{post.image_url && (
  <img src={post.image_url} alt={post.title} />
)}
```

## 8. 返信では画像を送らない

今回の設計では、返信には画像を許可しない想定です。

そのため、返信投稿処理では `image_key` と `image_content_type` を送らないようにします。

## 9. 投稿作成中の状態を管理する

画像アップロードが入るため、投稿ボタン押下後は以下の状態を分けて考えるとよいです。

```txt
署名URL取得中
R2アップロード中
投稿作成中
エラー
完了
```

## 10. エラー時の扱いを決める

たとえば以下のケースがあります。

```txt
署名URL取得に失敗
R2アップロードに失敗
R2アップロードは成功したが投稿作成に失敗
```

最初はシンプルに、失敗したらエラーメッセージを出して投稿作成を止める形でOKです。

## まとめ

フロントエンド側の中心は、投稿作成画面で以下の流れを作ることです。

```txt
画像ファイル選択
  ↓
署名URL取得
  ↓
R2へPUT
  ↓
投稿APIへimage_key送信
```

