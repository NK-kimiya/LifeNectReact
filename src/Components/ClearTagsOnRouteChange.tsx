// src/components/ClearTagsOnRouteChange.tsx
import { useEffect } from "react";
import { useLocation } from "react-router-dom";
import { useTagSelection } from "../Context/TagSelectionContext.tsx";

export function ClearTagsOnRouteChange() {
  //   const { pathname } = useLocation();
  //   const { clearSelection } = useTagSelection();

  //   useEffect(() => {
  //     if (pathname === "/article-search") {
  //       clearSelection();
  //     }
  //   }, [pathname, clearSelection]);

  return null;
}
